using Microsoft.AspNetCore.Mvc.Rendering;

namespace OnlineShop.Models;

public class FilterViewModel
{
    /*
    public FilterViewModel(List<Product> products, int product, string name)
    {
        product.Insert(0, new Product { Name = "All", Id = 0 });
        Products = new SelectList(products, "Id", product);
        SelectedProduct = product;
        SelectedName = name;

    }
    public SelectList Products { get; }
    public int SelectedProduct { get; }
    public string SelectedName { get; }
    */
}